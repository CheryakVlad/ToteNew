@model Common.Models.Tournament

@{
    ViewBag.Title = "Delete Tournament For Team";
    Layout = "~/Views/LayoutView.cshtml";
}

<h2>DeleteTournamentForTeam</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Tournament</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Team
        </dt>
        <dd>
            @ViewBag.Team.Name
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>    
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        <input type="hidden" value=@Model.TournamentId name="tournamentId" />
        <input type="hidden" value=@ViewBag.Team.TeamId  name="teamId" />
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "ShowTournamentsByTeam",new { id = ViewBag.Team.TeamId })
        </div>
    }
</div>
